package com.yungmac.dreamtime.init;

import com.yungmac.dreamtime.items.FoodBase;
import net.minecraft.item.Item;

import java.util.ArrayList;
import java.util.List;

public class ModItems {
    public static final List<Item> ITEMS = new ArrayList<>();

    // To add an item:
    // 1. Add a line below with the item and its name
    // 2. Add a texture in textures/
    // 3. Add an entry  in en_us.lang
    // 4. Add a .json   in models/
    // 5. Add a recipe  in recipes/

//    public static final FoodItem Example = new FoodBase("example", 4, 7f, true);
}
